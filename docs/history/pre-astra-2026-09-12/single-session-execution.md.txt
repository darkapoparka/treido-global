# Single-session Shop parity execution

## Current owner mode: GitHub only

Updated 2026-09-12 from the owner's explicit takeover instructions. This section supersedes the historical Windows checkout and parallel-lane instructions elsewhere in the repository.

- Repository: `darkapoparka/treido-bg`; branch: **main only**.
- Read and change source through GitHub tools. Use coherent multi-file commits when supported; preserve current parent and file SHAs and never force-update main.
- Execute installation, lint, typecheck, builds, tests, browsers and comparison scripts in non-deploying GitHub Actions checkouts of the exact evaluated commit.
- No Remote Desktop Commander, user computer, J: or M: checkout, local source checkout, worktrees, feature branches, pull requests or parallel writers.
- Downloaded Actions evidence may be inspected. That does not authorize a separate local source checkout or local app execution.
- No deployment, paid infrastructure, production data, live payments, secrets or external account operations.
- All Shop families across Tasks 3/5/6 remain in scope. Do not begin Treido branding, merchant/admin, backend or Expo work.

## Execution records

`shop-implementation-map.md` contains the current exact resume point and evidence boundaries. `shop-frame-ledger.md` is the frame ledger; `shop-parity-checklist.md` is the 97-flow acceptance record. Reuse the existing manifest, named scenarios, replay runner and ledger generator rather than creating competing plans.

The checked-in frame ledger predates the latest GitHub capture batch. The latest inspected Actions `frame-map.json` records **148 reproducible definitions and 276 route hints among 424 ordered frames**. This is not 148 visually completed screens. Regenerate the existing frame ledger through Actions when recording the next capture batch; retain unresolved and historical results explicitly.

## Latest preserved work

- `19ed8235a9ba1a95a090fc76518fabe39400f43f`: inspected media revisions after the first product-preparation failure. Later successful Actions runs prove the pipeline executed; subsequent media failures mean acquisition is not yet stable.
- `77efe3e2730c0cd65349eb4f0c6b529b1b8cb8e4`: complete committed Saved/collection implementation and replay/test batch. All 27 ordered frames in flows 7–13 have actual capture evidence, not just route mappings.
- `b12796d8a7fad047b306ed4a60e4c359317d31b4`: source-matched brightness treatment for acquired Rice/Argan photographs in the canonical Saved card. Web lint/typecheck passed; this change has **not** received rendered comparison because its run failed product acquisition.

The attempted `GitHub.update_file` change to `apps/web/src/features/discovery/components.tsx` was blocked by the connector before a commit. Do not assume that proposed dialog-history fix exists on main. A later unrelated Saved-card write succeeded; this was not a general loss of GitHub write access. Do not route the blocked operation through another interface or the owner's PC.

## Verified runs and open failures

### Saved implementation checkpoint

Actions run **34677545900**, evaluated commit `77efe3e2730c0cd65349eb4f0c6b529b1b8cb8e4`:

- Frozen references and allowlisted product preparation passed.
- Web lint and typecheck passed.
- All **52 selected frames** captured and scored: flows 2–13, 42 and 84.
- **46 interaction tests passed; 4 failed**. The Saved-specific file passed 8 of its 10 tests.
- Only `f005-004` and `f006-002` cleared both numerical thresholds. No frame or flow gained owner acceptance.
- Artifact **10292614218**, `shop-parity-77efe3e2730c0cd65349eb4f0c6b529b1b8cb8e4-1`, contains reports, frame map, interactions and reference/live/overlay/difference images.

Open interaction tests:

1. `nested billing Back and Cancel preserve the payment editor`: Back dismisses the payment editor as well as Billing address.
2. `filter state and scroll remain after nested Back, page Back/Forward and a product visit`: observed navigation to `about:blank` after an overlay Back.
3. `collaboration dismissal survives a real product navigation and return`: dismissal remains applied, but the compact action's accessible name includes the fallback avatar initial and fails the exact Invite collaborators locator. Correct the control's accessible name rather than weakening the assertion.
4. `More ideas Back, Forward and Done preserve collection scroll and focus`: expected scroll 0, actual 135. The test measures before Playwright scrolls the button out from under the dock. Establish a genuinely scrolled, unobscured click position, then preserve the exact scroll/focus/Back/Forward assertions.

### Latest source checkpoint

Actions run **34678202940**, evaluated commit `b12796d8a7fad047b306ed4a60e4c359317d31b4`:

- Frozen reference hashes, browser install, web lint and typecheck passed.
- Acquisition failed exact checksums for `rice-bundle`, `shower-caddy`, `home-air-dry-cream`, and `rice-shampoo`.
- Browser captures and interaction tests were **skipped**, not passed.
- Artifact **10292814536** contains the preparation log and rejected candidate images/report. Candidates are not served as verified assets. Do not automatically replace the expected hashes or lower verification requirements.

Foundation run **34677545892** failed `pnpm check` at Prettier on 18 files; the build and smoke steps did not run. Several formatting failures predate this batch and several were introduced by it. The later Saved-card formatting change is not evidence that the remaining failures were fixed.

## Continue without restarting the project

Resolve stable, provenance-verified acquisition for the four rejected product images. Preserve the failing gate and inspect source/candidate evidence; do not repeatedly repin changing bytes merely to produce a green run. Keep the shared-dialog blocked operation explicit. Correct the unblocked Saved accessibility/test-setup defects, and retain every failing assertion that describes required behavior.

The next complete coverage family is storefront/collections/filter/search/information/video: flows **14–16, 40–41, 96–97**. Flow 14's source montage has been inspected, but this session did not commit a new storefront implementation or replay. Inspect the other ordered sources and their existing owners before changing them. Other product/review, search/Minis, commerce, orders, onboarding and widget obligations remain open; do not narrow the assignment to this queue.

Use `.github/workflows/shop-parity.yml` and `scripts/shop-parity/run.mjs`; prefer complete related-family batches. Capture at 393×793, inspect pairs, test relevant 320/430 containment, commit actual code progress, and continue. A route, screenshot, passing lint, low mean score or prior-session claim is not completed 1:1 parity.

## Historical integration checkpoints

Preserve these existing implementations; do not reset main to them:

- `0f92453`: commerce/cart/checkout/order continuation.
- `f78ef18`: Account/Profile/People continuation.
- `4c9e902`: Home Deals/Notifications/Following continuation.
- `782d507`: Account payment/address quantitative checkpoint.

The old parallel-lane documents and local-only numerical reports are historical evidence, not current execution instructions or proof of current-main results.
