# Historical records, not agent instructions

`pre-astra-2026-09-12/` preserves byte-for-byte copies of the previous root documents as `.md.txt` snapshots, plus SHA-256 hashes. They came from commit `1d03706e84b5c19101246f2df42227bfddf78a54` before this documentation upgrade.

These files retain detailed measurements, old task evidence and prior operating modes for investigation. They are not current instruction files, approval records for new work, or an alternate backlog. Their embedded relative paths refer to the original repository root. For rendered original links, open the same path at that immutable commit on GitHub.

Do not load the archive at session startup. Read a precise dated section only when it supplies missing source observations or historical evidence. Current routing lives in [AGENTS.md](../../AGENTS.md), scope in [product.md](../../product.md), and execution in [tasks.md](../../tasks.md).

[Initial Astra setup validation](2026-09-12-astra-setup.md) records the earlier documentation-only batch. Later local setup, branch integration and Shop checks are recorded in the current status and existing parity evidence owners.

The legacy `treido-next` repository was inspected through its README and merchant contract as domain context. No private legacy source, schema, credentials or data was copied here. Its different multi-app deployment architecture is not adopted by this documentation update.
