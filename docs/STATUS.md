# Current project status

Updated 2026-09-21. This is the session entry point, not a second backlog.

## Resume handoff

Application checkpoint **b302d88** on local **main** follows Store/PDP **fe3d5e6**, Home/Deals **6b89210** and Account/Reviews/Saved/Cart **30f336e**. Search/Jeans and Sol retain drafts, Gift retains its saved collection, and Skin/wide-leg photo controls are connected. Subsequent local work repairs receipt/order/checkout variants and collection-stage history. Preserve this checkout and subsequent local work. The [copyable execution prompt](../single-session-execution.md#new-session-prompt) and [shared-component audit map](../shop-implementation-map.md#shared-component-audit) own continuation. Next: shared PDP/review finishing and Home accessory hearts, then continue unresolved receipt/order geometry and remaining source families.

The preview was found stopped on2026-09-20: no6412 listener or Treido Node process remained. The cause is unconfirmed. Restarted with installed Node24.19/Next16.3.4, `SHOP_REFERENCE_PREVIEW=1`, `VERCEL_ENV=preview`, loopback6412, hidden persistent process. Observed launcherPID42116/serverPID11960; recheck ownership instead of trusting these PIDs later. Startup reports ready, storefront/media return200, and the in-app Browser renders the mobile storefront. Logs: ignored `.qa/shop-parity/20260920-preview-restart.stdout.log` and `.stderr.log`. The [execution guide](../single-session-execution.md#existing-local-reference-loop) records the installed-runtime fallback because the pnpm shim was broken.

All 97 flows / 424 ordered frames are mapped and compared, and 307 standalone images are reconciled. The existing September 13 video audit actually inspected complete quarter-second sequence sheets for all 16 videos (309 samples, 4,310 decoded frames); the old “poster-only” description was stale. This session also examines selected full-rate motion. Coverage, motion inspection and passing tests are separate from 1:1 acceptance.

## Active repair checkpoint

**b302d88** has scoped evidence, not a new complete qualification. Search/Mini adds49 unique frames,7/7 new journeys and25/25 final router siblings; earlier40/40 siblings cover unchanged owners. Subsequent local commerce/collection work adds reversible collection stages, receipt/payment presentation, source-specific checkout/order recommendation media and return-path fixes. `20260921-commerce-collection-dirty` improves the scoped 21–31/60–68/79 mean by **0.08345 MAE points** versus the complete baseline; receipt top improves **4.485→2.975%** and receipt tail **4.203→3.948%**. The largest scoped regression is **+0.118**, below the 0.15 guard. Focused checkout/order journeys pass **48/48**, collection-stage journeys **9/9** at 320/393/430, and TypeScript/scoped ESLint pass. The [implementation map](../shop-implementation-map.md#current-source-checkpoint) records exact precedence. No full build, units or complete424 capture was repeated. The complete ledger below is unchanged.

The current Search/assistant fitting keeps the photo and Jeans answers in their canonical live components. Final scoped evidence improves the captured photo answer **5.596→5.284%**, expanded steps **5.382→5.073%**, comparison viewport **5.833→5.545%**, and Jeans answer top **5.229→5.002%**. The tail now uses the source-confirmed wording “garage setting”; its whole-frame score moves **4.433→4.450%** despite correct copy, wrapping and geometry. All 16 flow45–47 states recapture without replay/browser errors, **28/28** focused journeys pass, and TypeScript, scoped Next ESLint, Prettier and diff checks pass. Probe fonts, sources, masks, hashes, recipes and thresholds remain unchanged. This is scoped progress, not a complete 424-frame qualification or 1:1 acceptance.

The current Pura hide/Undo repair replaces the synthetic concealed-card approximation with a bounded crop from frozen flow 42 while removing the captured eye, message, Undo and toast ink so those controls remain live DOM. Final run `20260921-pura-owner-text` scores all **4/4** states with zero replay/browser errors: the reason sheet improves **2.822→1.647% MAE**, hidden/Undo improves **4.590→1.344%**, and family mean improves **2.811→1.706%** without changing the two earlier siblings. The full Home overlay suite passes **13/13**; the strengthened Pura-focused rerun passes **2/2** after asserting the exact hidden-photo owner. Browser checks at **320/393/430** preserve the real 630px card, keep the 633px source field and live controls contained, and introduce no horizontal overflow. TypeScript, correctly scoped app/test ESLint, Prettier and diff checks pass. This remains scoped current-head evidence, not a full 424-frame qualification or acceptance.

The current Chemical Guys owner fitting extends **aab88f3** with source-matched category ink, pinned-header transition, recommendation typography, video labels, Featured geometry and a delayed blurred dock fade. The scoped Geist variable font is the exact Latin asset bundled with the installed Next runtime and is used only by the Chemical storefront/pinned header. Final run `20260921-chemical-owner-final` captures **8/8** flow 15/96 states with zero replay/browser errors. The shared products/video frame improves **4.553→2.654% MAE** in both flows and the video detail improves **1.475→1.439%**; the eight-frame mean improves by **0.48485 points** against `20260920-connected-qualified-full`, with every sibling unchanged or slightly improved. `store-media-journeys.spec.ts` plus `storefront-journeys.spec.ts` pass **17/17**. Direct `20260921-chemical-widths.json` checks show no document overflow and keep the dock and recommendation panel contained at **320/393/430**. CSS Prettier, `git diff --check` and the served font check pass. This is scoped current-head evidence, not a complete 424-frame qualification or owner acceptance.

The current payment/card owner fitting repairs the complete flows 80-82 family in the canonical Account components: profile and add-card canvas/shadows, scrolled field geometry, saved-card privacy treatment, two-card stacking, card detail alignment, and the connected delete confirmation. Final run `20260921-payment-owner-fit9` captures **13/13** states with zero replay/browser errors; family mean improves **2.172 -> 2.020% MAE**, the scrolled add-card frame **3.046 -> 2.238%**, the delete confirmation **3.499 -> 3.300%**, and the three single-card Profile siblings **2.014 -> 1.894%**. The focused Account suites pass **16/16**, including 320/393/430 containment, saved billing association, card identity, delete history and return behavior. Next type generation, TypeScript, correctly scoped web ESLint, Prettier and `git diff --check` pass. This is scoped current-head evidence, not a complete 424-frame qualification or 1:1 acceptance.

The complete production-preview capture at clean **b8377ff** (application **4bda84e**), **20260920-connected-qualified-full**, covers **424/424** frozen frames: zero replay/browser errors, **2.765997% mean MAE**, **8.636994% mean bad pixels**, and **57 numerical candidates**. No frame regresses more than 0.15 MAE points against complete f77609c. The [frame ledger](../shop-frame-ledger.md) owns this exact run. **367 frames exceed a numerical threshold; owner acceptance remains 0/97. The entire app is not 1:1.**

Application **4bda84e** passes the isolated production build (39 pages), TypeScript, app lint and **132/132 unit tests**. The full reference suite initially passes **357/358**; the sole failure was an outdated assertion hiding the lower Jeans merchant captions despite visible source f046-002. Test-only **b8377ff** preserves their exact text, visibility and source-gray color; the exact rerun passes **1/1** against the unchanged build. This is 358 distinct passing journeys across the initial run and rerun, not a clean first-pass suite. Original report/trace, rerun, units and build evidence remain in `.qa/shop-parity/20260920-connected-qualified/`.

The active batch repairs Orders/manual-order carriers, forms, history, reversible deletion and review controls; Deals and Pura share one working options/report menu. Store/PDP/review sheets preserve stage, selection, focus and Back/Forward. Checkout edits the selected masked card and billing association, restores source-specific payment rows and Cash visibility, and exposes real local help states. Phone/profile continuity, public collections, photo drafts, Support drafts/scroll, Filters and Explore returns use their existing state owners.

Shared card navigation now restores the exact control, vertical page position and horizontal rail, including a recently viewed item that genuinely changes position. Search and StoreSearch retain their draft/editor through suggestion navigation; Cancel/submit discard the edited draft correctly. Browser confirms these returns at 393px and affected 320/430 widths. A gated, hash-verified local video owner adds actual Kitsch, tracking, empty Orders and bounded Sol motion with pause/error/reduced-motion behavior. Eight playback/motion journeys pass; 13 width captures have no overflow or browser errors. Full media verification still reports the same three inherited mismatches, with original expected hashes preserved.

Retained scoped comparisons improve archive **3.145 → 1.403% MAE**, purchase history **3.340 → 1.512%**, Pura options **4.465 → 1.539%**, expanded payment **3.343 → 3.102%**, and Beauty ending **4.88 → 4.00%**. Source-measured Explore/Look/first-collection corrections and Sol's per-frame decorative masks are implemented; final scoped runs score 27 and 19 frames with zero browser/replay errors. All seven new video derivatives verify, and four updated playback journeys pass. Root inspected the reference/live pairs and actual Sol flip captures. All 755 frozen source files verify; all 16 standalone MP4s have zero audio tracks. Combined app lint, TypeScript, formatting and **132/132 unit tests** pass. Exact run precedence, original failures and rejected probes belong to the [implementation map](../shop-implementation-map.md#current-source-checkpoint). Orders now retain manual drafts through Help and preserve locally changed delivery status through Back/Forward; review Close and ordinary route returns restore their real origins. Mini Close crosses its owned query stages to the original row or Search trigger. Search/StoreSearch Cancel and photo-answer Close restore the labeled form. Fresh Browser checks pass the complete Mini → product → Back → Close path and Search/photo cancellation. Explore/collection final fitting scores19/19 without errors, improving Explore003 from4.012 to3.450% and004 from4.474 to3.868%; empty-cart visibility follows the source category block while populated carts remain available. The final continuity pass also retains Pickup choices through payment settings, returns uncommitted StoreSearch to its actual opener, and unifies Home merchant reporting and Visit return behavior. Sixteen distinct focused journeys pass. A selected-photo editor regression found during capture is repaired: 5.271→3.788% MAE, with seven siblings unchanged. Final Orders field/timeline alignment and Chemical text corrections have six passing journeys and 16 clean captures; the checkout address-menu correction improves 4.712→3.363%. Root inspected the source/live pairs and fresh Browser returns. The complete build/journey/capture evidence above now supersedes this scoped evidence; the next shared repairs remain open.

### Preceding repair evidence

The connected-controls batch over **dba6636** repairs clipped birthday entry, Connections/provider spacing and glyphs, Orders action icons and timeline markers, review stars, unavailable Mini history, Women category disclosures, feedback's bounded source photograph, and Minis header/shadow rendering. Checkout restores card-field geometry and CVV help, the address phone-help action and accessible footer, and expanded billing spacing. Onboarding now opens Gmail directly, uses the existing real legal destinations, and restores the updates screen's source copy/layout. Browser checks include393px and320px; narrow address buttons stay on one line, and the updates card fits its status text. Native Windows scrollbars no longer steal mobile sheet/answer width.

Scoped evidence covers **94 unique frames**, zero replay/browser errors and no >0.15-point regression against24b0143. Card setup improves **2.919 -> 1.290% MAE**, empty address **4.302 -> 1.657%**, updates onboarding **2.991 -> 1.495%**. The27 distinct focused journeys pass after one Saved-navigation rerun: its original trace shows the required page chunk still loading during HMR, so an explicit URL checkpoint was added while retaining the product assertion. TypeScript, scoped source/test ESLint, formatting and diff checks pass. Exact run precedence, original failure evidence and bounded unavailable states are in the [implementation map](../shop-implementation-map.md#current-source-checkpoint). The completef77609c capture above now supersedes this partial evidence.

The responsive controls batch over **5288f41** fixes the doubled Kitsch Follow capsule reported at467px: clean stills from the existing original video replace screenshot backgrounds containing control-shaped holes. Logo, rating and all controls are live elements. The shared dock remains compact at wider mobile widths, with corrected Back/cart/Share/navigation glyphs and store action spacing. Following's missing quilt heart and Saved's missing empty-cart action are repaired.

Cart now has a full-width black backdrop, rounded product preview/handle, saved-item merchant badge and canonical offer/quantity icons. Unprofiled Buy now and offer continuation enter phone setup consistently. Saved's Create action no longer pushes down its shelf; collection thumbnails and photo proportions follow the source. Browser inspection covers472px cart/store/Saved behavior; focused journeys cover320/393/430/472 containment and history. **26/27 initially pass**; the remaining failure exposed a real text-layer interception of the collaboration dismiss button. Its repaired hit target passes the exact rerun, yielding27 distinct passing journeys. TypeScript, scoped ESLint and formatting pass.

Final scoped evidence covers **77 unique frames**, zero replay/browser errors and no >0.15-point regression against24b0143. Filled cart improves **4.398 -> 2.319% MAE**, saved-for-later **3.794 -> 1.633%**, populated Saved **4.956 -> 3.196%**. White fades that washed out Following photos and leaked into the keyboard editor were rejected and corrected before retention. Both new original-video derivatives verify; the same three inherited original-media mismatches remain untouched. Exact run ownership and unresolved cart-underlay differences are in the [implementation map](../shop-implementation-map.md#current-source-checkpoint). Dev remains on6412. No full424 capture, production build, deployment or1:1 acceptance is claimed. Continue the next source families within this request.

The current surface-color batch restores the source **#fdfdfd** canvas for product underlays, Notifications, order review, and phone/card setup. Address-setup bodies and checkout review remain their captured white; an initially broader setup rule regressed four address frames and was narrowed before retention. Setup headers/footer and card CTA colors are fitted separately. A source-measured saturation correction applies only to the newly delivered manual-order Peach Bee photograph; label-created and later-history screenshots remain identical.

Scoped evidence covers **61 unique frames**, zero replay/browser errors and no >0.15-point regression against the complete24b0143 baseline. Notifications improves **2.755 -> 2.053% MAE**, phone processing **1.605 -> 0.835%**, card setup **2.919 -> 2.253%**, review states **2.068/2.012/1.949 -> 1.377/1.318/1.254%**, manual delivered **5.208 -> 4.960%**. Focused product, phone/address and order journeys pass **25/25**, including320/393/430 widths. Formatting, diff and documentation checks pass; CSS-only work did not require another unit/type/build run. The implementation map records which final run owns each frame; the full-run ledger below is unchanged.

Dress fitting **1254cef** restores its captured plain-purple offer bag, darker purchase gradient, broader shadows and source content background: **5.035 -> 4.265% MAE** over36c57de. Its captures cover9/9 siblings plus the final background frame; product/ratings journeys pass **7/7**. The current batch moves the shared underlay color into its canonical owner and removes the redundant Dress override. This remains above the unchanged1.5% threshold.

Cart continuity **36c57de** repairs the observed full-reload reset required by `web.md`: product/variant selections, quantities and saved-for-later lines survive reload in the current tab, including an intentionally empty cart. Cart and later transitions share one atomic, validated snapshot, isolated by reference scenario and seed. Amounts still resolve from the catalog; payment/account state is not persisted. Direct in-app Buy now -> checkout -> reload retains the Bag and **$10.82** total. Independent read-only review found no behavioral regression.

Product headings, prices and description text now use the same scoped grayscale compositing as checkout. Final development capture **20260920-cart-pdp-final** scores **31/31**, zero replay/browser errors and no >0.15-point regression against the complete 24b0143 baseline. Dress **5.101 -> 5.035% MAE**, Bag **3.567 -> 3.536%**, Shea description **5.641 -> 5.617%**. Focused cart/checkout journeys pass **11/11**, plus the preceding cart-presentation/account siblings **9/9**; the initial new removal test used Remove while quantity was still two, and was corrected to exercise Decrease then Remove. Cart model units pass **12/12**; TypeScript, changed-source lint and formatting pass. These scoped results do not replace the full ledger below.

The preceding **2d5df56** visual batch corrects Explore’s photograph crop/shadow, checkout summary structure and processing opacity, and review text/menu dots: Explore **5.369 -> 3.260% MAE**, expanded checkout **3.998 -> 3.479%**, processing **4.857 -> 4.510%**, product/store reviews **4.937/4.449 -> 4.763/4.360%**. Its final captures score **36/36** plus **2/2**, and connected journeys pass **22/22**. Browser review covered Explore at393px and checkout at320/430px. Memory-exhaustion failures recovered after the user-authorized stop of unrelated automarket-astra-pro and gym-fidelity servers; Treido stays on **6412**.

The preceding Windows production capture at **24b0143**, **20260920-tracking-qualified-mobile**, covers **424/424** frames, zero replay/browser errors, **3.045956% mean MAE**, **9.423731% mean bad pixels** and **40 numerical candidates**. No frame regresses more than0.15 MAE points against the previous complete af59e39 run (mean3.098600%). Its production-build evidence remains valid for that source; the ledger has advanced to the newer development capture above.

The isolated production build passes TypeScript and generates 39 pages; units pass **106/106**. Production journeys initially pass **281/284**, with no retries or skips. Dress reviews failed only an obsolete boundary-copy expectation; that assertion now matches the implemented text while retaining product identity, count, absent unrelated reviews and Back checks. Review hydration failed because Chromium refused a required JavaScript chunk with `net::ERR_INSUFFICIENT_RESOURCES`; a Saved image-decoding wait also timed out despite completed image responses. All three exact journeys pass **3/3** against the same production build on rerun; the two timing failures are retained, not reported as a clean first pass. Their original traces and JSON results are preserved under ignored `.qa/shop-parity/20260920-tracking-qualified-mobile/`. The user development server on 6412 remains untouched; production verification used 6413 and installed Chrome 153.

The complete capture includes the recent Home photograph/header/heading, collection identity, photo-answer and tracking corrections. Bag Read all reviews preserves its own **3.8K** aggregate instead of showing Shea's **3.3K** reviews, with an explicit boundary for the uncaptured full list. Tracking cards use source **#c2bfb5**, an independently fitted photograph fade and a 7px upward photo offset. Waiting and label-created tracking improve **5.299/5.181 -> 3.964/3.770% MAE**. Exact ownership, source coordinates and scoped interaction evidence are in [the implementation map](../shop-implementation-map.md#current-source-checkpoint).

One static Chemical Guys frame crosses the numerical threshold in the full run (**1.475 -> 1.595%**). A same-build repeat reproduces the original screenshot byte-for-byte at **1.474546%**, with matching served media hash and dimensions. Its intermittent raster difference remains unexplained; the ledger retains the actual full-run score. The interrupted earlier **20260920-home-qualified-mobile** run has no final report and is not qualification evidence.

All **755 frozen corpus files** passed the last manifest check. Separate original-media verification still fails for `store-hero`, `shower-caddy` and `rice-shampoo`; assets and expected hashes are unchanged. The earlier macOS run **35475467664** at **88e38df** captured all 424 frames and passed 282 journeys, then failed owned-server cleanup and the unchanged numerical gate. The cleanup repair has six passing mocked lifecycle cases; a real macOS rerun remains pending publication. The old push stopped before publication, its credential selector is no longer running, and remote main was last verified at **88e38df**. No credentials changed or replacement push started.

Continue the ordered Browser/source audit under Tasks3/5/6. Birthday, account, checkout and Mini controls have been repaired above; remaining typography, manual-order geometry/confetti and photo-answer differences stay open. Unidentified Explore/order recommendation fragments, native motion and source approval remain unresolved.

## Dated September 18 fitting evidence


**Skincare cleanser source-card checkpoint over `40ba9e5`.** The recorded Cleanser grid now uses four bounded source cards with the exact product scale, neutral backgrounds, Allure/New badges and live heart controls. Product links and Save state remain canonical DOM behavior; the source cards supply photography only. The results surface also restores the captured rounded top edge and clipped, non-ellipsis titles.

Run `20260918-skin-source-cards` scored **7/7** flow-57 states with zero browser errors. Family mean improved **5.259% → 4.653% MAE**, mean bad-pixel **19.348% → 15.265%**, the cleanser grid improved **7.104% → 4.558%**, and the recorded summary improved **5.845% → 4.145%**. The production build passed TypeScript and generated 39 routes; `minis-photo-journeys.spec.ts` passes **5/5 (13.6s)**. This remains measured progress, not 1:1 acceptance.

**Kitsch store-search source checkpoint over `a1fba86`.** The captured shampoo results now keep their four identified live products, restore the two bounded lower-card source continuations hidden beneath the Shop dock, use an actual DOM chevron instead of corrupt text, and continue to preserve real query/filter/Back/Forward behavior. No unseen product identities or destinations were invented.

Run `20260918-store-search-source-tail-retained` scored **4/4** flow-40 states with zero browser errors. Family mean improved **3.910% → 3.454% MAE**, mean bad-pixel **13.891% → 12.130%**, and the final result frame improved **7.518% → 5.420%**. The production build passed TypeScript and generated 39 routes; the full storefront suite passes **11/11 (21.2s)**. This remains measured progress, not 1:1 acceptance.

**Chemical Guys source-layout checkpoint over `b527a06`.** The storefront now uses the captured page/panel tones, source-measured category widths, and the visible fourth video continuation rather than ending the rail early. The fourth item remains an unavailable local preview with bounded source photography; no video, audio, or product identity was invented.

Run `20260918-chemical-source-layout-final` scored **8/8** flow-15/96 states with zero browser errors. The Chemical products/video frame improved **8.101% → 6.033% MAE** in both owning flows; store-video family mean is now **3.754%**, and store-browsing mean is **4.730%**. The production build passed TypeScript and generated 39 routes, while `store-media-journeys.spec.ts` passes **4/4 (6.1s)**. This is another retained reduction, not full 1:1 acceptance.

**Reported-product and store-tail source-fitting checkpoint over `838c600a2bf5837ebecfa3e9e3150ea56cba3e27`.** The final report state now carries the captured saved Rice bundle, cart-free dock, exact confirmation copy, source-aligned scroll position, and a bounded source crop for the concealed Shea card while retaining the live DOM status mark. Store filtering and the reported storefront also use the exact captured lower product-card fragments rather than unrelated complete product photography.

Run `20260918-reported-shea-exact` scored **11/11** flow-16/38 states with zero browser errors. Product-reporting mean improved from **2.895% to 2.045% MAE**, and its final confirmation frame improved **9.984% to 4.881%**. Store-filter mean improved from the earlier **4.415% to 3.408%**, with the final grid at **4.393%**. The production build passed TypeScript and generated 39 routes; `storefront-journeys.spec.ts` passes **11/11 (25.9s)**. These remain measured reconstruction gains, not full-app 1:1 acceptance.

**Explore source-fitting checkpoint over `e92763b`.** Explore now uses recovered full source cards for Home, Menswear and Beauty shelves, the complete source hero with bounded interface-ink reconstruction, source-matched department colors, and root-Explore typography/control fitting. Product links, Save controls, category routes, Minis navigation and the Beauty-specific composition remain live; unavailable continuation identities were not invented.

Run `20260918-explore-systemic-flow50` scored **4/4** Explore states with zero browser errors at 393x793 on the isolated Node 24.21.0 production preview. Mean MAE improved **5.924% -> 5.004%** and mean bad-pixel **20.820% -> 17.974%**. Frame MAEs are **4.699 / 5.435 / 5.184 / 4.698%**; the categories frame improved **6.549% -> 5.435%** and the Minis/Home-shelf frame **7.219% -> 5.184%**.

The companion Beauty run `20260918-explore-systemic-flow51` scored **6/6**, zero browser errors, and improved family mean **5.533% -> 5.349%** without applying the root-only typography override to Beauty. The production build passed TypeScript and generated 39 routes; `explore-entry-journeys.spec.ts` passes **6/6 (33.9s)** across 320/393/430 widths, exact source-card selection, Minis history, Beauty state and empty-cart behavior. These screens remain above the unchanged 1.5% threshold, so this is measured progress rather than 1:1 acceptance. [The implementation map](../shop-implementation-map.md#explore-source-fitting-checkpoint-2026-09-18) owns exact evidence and rejected probes.

**Get the Look source-fitting checkpoint over `758a96a10c36326b33974785ee359cff7dc3174c`.** The recorded outfit flow now keeps the Mini wordmark on the live gradient, uses a phase-specific deskewed source photograph for result states, restores the complete captured blazer/shirt product media, compare-at prices and 150x200 card geometry, and preserves the source-bounded third-card continuations. Hotspots, selection, product links, Save controls, Back/Forward and the all-matches scroll remain live DOM behavior.

Run `20260918-get-look-deskew` scored **8/8** flow-58 states with zero browser errors on the isolated Node 24.21.0 production preview. Family mean improved **5.691% -> 4.741% MAE** and mean bad-pixel **17.775% -> 13.302%**. The all-matches frame improved **8.601% -> 4.524%**, the outfit-hotspot frame **7.034% -> 4.957%**, and the selected-shirt frame **7.260% -> 5.814%**. The production build passed TypeScript and generated 39 routes; the focused Minis/catalogue/auxiliary suites pass **19/19 (42.7s)**. These states are substantially closer but remain above the unchanged 1.5% threshold, so they are not 1:1 acceptance. [The implementation map](../shop-implementation-map.md#get-the-look-source-fitting-checkpoint-2026-09-18) owns exact source regions and evidence.

The clean complete `20260918-full-e2c3ee9` comparison scored **424/424** states with zero browser errors, **3.511% mean MAE**, **11.024% mean bad-pixel**, and **9.984% worst-frame MAE** at commit `e2c3ee9`. It predates the filtered-Jeans batch below, so another same-source complete capture remains required after additional shared work.

**Storefront source-fitting checkpoint over `f8820e98b61beccf97d1d75662be8e657b821970`.** Kitsch now uses the captured default and followed storefront hero photography while retaining live menu, search, Follow and share controls. The What's New collection keeps live title/share/filter controls over its recovered source hero; duplicate captured ink is reconstructed with bounded diffusion rather than visible transparent blocks. Its selected In-stock badge, share position, promotion typography, recommendation media and collection cards now follow the recorded source while navigation and filtering remain connected.

Run `20260918-store-header-badges-retained` scored all five flow-14/41 states with zero browser errors on the isolated Node 24.21.0 production preview. Default/followed storefront MAE improved from **6.245 / 8.857%** to **3.418 / 3.545%**; family mean improved **7.551% -> 3.482%** and mean bad-pixel **36.884% -> 12.464%**. Flow 14 now measures **4.984 / 4.601 / 5.470%**. The complete storefront journey suite passes **10/10 (18.9s)**, and the production build passed TypeScript and generated 39 routes. These screens are substantially closer but remain above the unchanged 1.5% threshold, so this is not 1:1 acceptance. [The implementation map](../shop-implementation-map.md#storefront-source-fitting-checkpoint-2026-09-18) owns exact source regions, runs and remaining gaps.

**Home source-fitting checkpoint building on `0ce33c8452488bfc516571ff33bc49969ab35583`.** The Home feed now preserves recovered source-photo slivers for Princess Polly, Loaded Tea and Accessories, uses selective light/dark ink removal instead of broad rectangular erasures, restores the full captured DRMTLGY recent-store card on Home, fits the shared filled cart glyph, matches the shortcut row more closely, restores the dark backing behind the captured accessories price region, and refines email-card letter spacing. The two captured accessory hearts remain non-interactive DOM visuals because their product identities are unavailable; no catalog record or save state was invented.

Run `20260918-home-polish-2-final` scored **6/6** flow-2 states with zero browser-error rows at 393x793 on the isolated Node 24.21.0 production preview. Mean MAE is **3.855%** and mean bad-pixel is **15.138%**, versus the documented Mountain checkpoint's **4.555%** mean. Frame MAEs are **4.685 / 3.572 / 3.170 / 4.932 / 4.099 / 2.669%**. Every frame still exceeds the unchanged 1.5% threshold, so this is measured progress, not owner acceptance or 1:1 completion.

The isolated production build passed TypeScript and generated 39 routes. `home-discovery-continuity.spec.ts` plus `home-overlays.spec.ts` pass **18/18 (22.4s)**, including the full Home DRMTLGY card, shortcut containment, filled cart, accessory source hearts, connected save behavior, overlay dismissal and Back/Forward restoration. [The implementation map](../shop-implementation-map.md#home-source-fitting-checkpoint-2026-09-18) owns the exact source changes, rejected probes and frame comparison. Correctly scoped web/test ESLint, Prettier, documentation integrity and `git diff --check` also passed. Full application suite, CI, motion review and the outstanding same-source full capture remain open; owner acceptance stays **0/97**.

**Search answer source-fitting checkpoint over `aeba61229ef278503c957a27b5eba9ad184faaf4`.** The Jeans answer now preserves the captured partial recommendation media and wide continuation, uses circular control cutouts rather than square holes, fits the Edit search glyph and answer typography, and keeps the real product links, Save actions, feedback sheet and Back/Forward history live. Opening an answer now records a session-fresh conversation as “Just now”; seeded captured history retains its dated label.

Run `20260918-search-answer-recency-final` scored **16/16** selected flow-45/46/47 states with zero browser errors. Across the intersection with `20260918-full-current`, mean MAE improved by **0.269 percentage points**. Search-answer mean improved **5.773% -> 4.930%**, feedback mean **4.919% -> 4.558%**, and the answer-sheet top improved **8.258% -> 6.226%**; the photo-answer siblings remained effectively unchanged within +0.008 points per frame.

The isolated production build passed TypeScript and generated 39 routes. The complete focused Search suite passes **18/18 (26.8s)** after the conversation-recency correction, covering answer history, source continuations, feedback, filters, photo drafting, recent items, Save state and mobile containment. These frames remain above the unchanged 1.5% threshold, so this is measured progress rather than 1:1 acceptance. [The implementation map](../shop-implementation-map.md#search-answer-source-fitting-checkpoint-2026-09-18) owns exact changes and frame evidence.

**Filtered Jeans source-fitting checkpoint over `e2c3ee93ab6878a1d51d50ffc60d0ea93a2f3479`.** The final flow-48 result now uses the recorded Arrow Twenty Two and American Blues merchant cards, Valentino and Givenchy result identities, exact visible prices and source media, a bounded third-store continuation, the captured related-products row, and a stroke-only selected Filter glyph. Reference-only products retain zero sellable quantity rather than inventing stock; unrelated local products are no longer relabeled to imitate the source.

Run `20260918-filtered-jeans-final` scored **10/10** flow-48 states with zero browser errors. Family mean improved **3.537% -> 3.024% MAE** and the final filtered result improved **9.400% -> 4.370%**; every sibling frame also improved by 0.010-0.018 points through the shared Filter icon correction. The isolated production build passed TypeScript and generated 39 routes, and the complete focused Search suite passes **19/19 (24.6s)** across exact source composition, filters, answer history, photo drafting, Save behavior and 320/393/430 containment. These states remain above the unchanged 1.5% threshold, so this is measured progress rather than 1:1 acceptance. [The implementation map](../shop-implementation-map.md#filtered-jeans-source-fitting-checkpoint-2026-09-18) owns exact evidence.


**Store-filter source-grid checkpoint over `ccacb13`.** The exact KITSCH On sale + $380 state now renders the six products visible in the frozen source instead of collapsing to the unrelated Summer Mystery Box. This eligibility is isolated to the recorded filter combination and does not invent compare-at prices or alter ordinary sale-only filtering. The native maximum-price slider retains keyboard focus without the browser's rectangular focus ring.

Run `20260918-store-filter-source-grid` scored **5/5** flow-16 states with zero browser errors. Family mean improved **4.415% -> 3.536% MAE**; the final filtered grid improved **9.075% -> 5.023%**, and the $380 slider state improved **3.182% -> 2.843%**. The production build passed TypeScript and generated 39 routes; the combined storefront/media suite passes **14/14 (29.8s)**. All frames remain above 1.5%, so further product-card typography/media fitting remains open.

**Skincare permission-source checkpoint over `325e7eb`.** The camera-permission state now uses the captured warm-to-mint surface, source heading face, symbol spacing and Analyze-button placement only while that local permission sheet is open. The camera/network boundary remains explicit and no device permission is requested.

Run `20260918-skin-permission-final` scored **7/7** flow-57 states with zero browser errors. The permission frame improved **6.561% → 3.275% MAE** and family mean improved **4.653% → 4.183%**, with no sibling regression. The production build passed TypeScript and generated 39 routes; `minis-photo-journeys.spec.ts` passes **5/5 (14.5s)**. This remains measured progress, not 1:1 acceptance.

**Pura not-interested source checkpoint over `d19c914`.** The reason stage now switches to its own captured video still, dark header region and Pura wordmark while preserving the real Follow, hide and Undo interactions. The reason sheet is aligned to the captured vertical position; no unseen motion or merchant data is fabricated.

Run `20260918-pura-source-final` scored **4/4** flow-42 states with zero browser errors. The reason frame improved **6.817% → 4.111% MAE** and family mean improved **4.811% → 4.134%**. The production build passed TypeScript and generated 39 routes; `home-overlays.spec.ts` passes **13/13 (16.8s)**. This remains measured progress, not 1:1 acceptance.

**Expanded search-history dock-fade checkpoint over `e687ae6`.** The full Recently viewed grid now retains the captured viewport-edge fade behind the live dock instead of exposing fully opaque lower cards. The fade is DOM/CSS presentation only; all eight cards, save/remove controls and navigation remain live.

Run `20260918-search-history-fade-final` scored **2/2** flow-49 states with zero browser errors. The expanded history frame improved **6.232% → 5.695% MAE** and family mean improved approximately **3.819% → 3.550%**. The production build passed TypeScript and generated 39 routes; `search-history-journeys.spec.ts` passes **2/2 (3.8s)**. This remains measured progress, not 1:1 acceptance.

**Store-information source-layout checkpoint over `61c3c12`.** KITSCH information categories now use two equal minmax columns instead of image-intrinsic grid sizing, and the storefront/information ratings use the real star glyph instead of mojibake. All category links and lower bounded fragments remain live.

Run `20260918-store-info-layout` scored **5/5** flow-97 states with zero browser errors. The broken category frame improved **14.448% → 5.878% MAE** and family mean improved **5.928% → 4.206%**. The production build passed TypeScript and generated 39 routes; `storefront-journeys.spec.ts` passes **12/12 (28.2s)**. This remains measured progress, not 1:1 acceptance.

**Storefront text-integrity checkpoint over `fffaa63`.** Corrupted promotion codes, chevrons, rating stars, review separators, description punctuation and video ellipsis in the shared storefront owner were replaced with the intended source text. No offer validation, external merchant data or playable media was added.

Retained runs `20260918-store-text-f14`, `-f15`, `-f96` and `-f97` scored **16/16** frames with zero browser errors. KITSCH expanded promotions improved **6.125% → 5.032% MAE**; the collection promotion frame improved **5.011% → 4.468%**; information siblings f097-001/003/004 improved to **3.418% / 2.720% / 3.078%**. The production build passed 39 routes and the combined storefront/media suites pass **16/16 (31.3s)**. This is not 1:1 acceptance.

## Working context

Use the existing `J:\treido-bg` checkout of `darkapoparka/treido-bg`, canonical **main**, with one writer. The historical September 13 integration extended **`4d0539f2d0cd70f884941cc83f05ff075365542d`**; the active September 19 repair is recorded above. The local writer owns all current edits. [AGENTS.md](../AGENTS.md) and [Codex setup](agents/codex.md) own branch policy, runtime ownership and synchronization.

The active work remains the Shop **mobile-web** portions of Tasks 3/5/6. Finish and approve the frozen UI and connected flows, then adapt the same implementation to the producer-first food marketplace under [product.md](../product.md) and [web.md](../web.md). Backend, native, merchant/admin, food branding and release remain separate work.

## Dated September 13 implementation checkpoint

All **97 flows / 424 ordered states** now have executable recipes, and every ordered source frame has been visually inspected. The complete `20260913-complete-current` run scored **424/424 states with zero browser errors**, with **31 numerical candidates**, mean **4.817% MAE** and 33 states above 10%. Subsequent fitting changes require their own comparisons; the full run is a dated baseline. Owner acceptance remains **0/97 flows**. [The implementation map](../shop-implementation-map.md) identifies canonical family owners and source gaps; the [frame ledger](../shop-frame-ledger.md) and [flow checklist](../shop-parity-checklist.md) remain the measurement and acceptance owners.

The batch connects onboarding and real loading fallbacks, Home/search history, assistant photos and feedback, checkout editors and receipts, manual orders/tracking, Minis, and account continuations through the existing components and state. Home and Search share recent items. Profile exposes manually created orders independently of profile completeness. Phone entry preserves one country code through both address paths and Back. Store information, product entry, photo menus, order panels and Beauty have source-specific geometry and actual photo/brand regions; controls remain live DOM.

The latest full interaction run passed **206/207** tests (`20260913-interactions-integrated.json`). Its only failure was an expectation missing the source's space before a review-text period; the corrected review and checkout regressions passed **21/21**, including two new phone-to-address journeys. Explore/media siblings passed **5/5**. The complete suite now contains **210** tests; a single full run on the final commit remains pending. Final `pnpm check` passed lint, formatting, all three package typechecks and **17 unit tests**; the real-loading replay helper passed **4/4** tests. Documentation integrity and `git diff --check` passed.

The closing source audit restored the Shop splash wordmark, matched new-onboarding code colors, centered checkout Close/Back controls, retained the single-line payment address and corrected six captured review-author labels. Onboarding/account siblings passed **12/12** and checkout/review siblings **14/14**. `20260913-checkout-fifty-final` scored **50/50 states**, zero browser errors, mean **4.188% MAE**, zero numerical candidates. `20260913-onboarding-final` scored **24/24**, zero browser errors, mean **2.413% MAE**, eight numerical candidates; the restored splash is **0.332% MAE**. The earlier native/browser exceptions and provider boundaries remain explicit.

Flow 19 now records the later offer/arrival/rating snapshot as an explicit entry before the real Save action. It preserves product prices, stock and cart calculations; Save changes only membership. The final product/account sibling run passed **16/16**. `20260913-product-fifty-final` scored **50/50**, zero browser errors, mean **3.919% MAE**, five numerical candidates. These are focused final-source comparisons, not a replacement for the pending complete CI comparison.

`20260913-final-fifty-reviewed` compared another **50 states**, all scored with zero browser errors. Examples improved from the earlier full baseline: store video 18.357% Ã¢â€ â€™ 1.475% MAE, skin photo menu 26.732% Ã¢â€ â€™ 4.285%, and expanded order detail 10.751% Ã¢â€ â€™ 4.583%. These are measured improvements, not accepted screens. The runner now waits for decoded images before positioning scroll anchors. Final Beauty/phone run `20260913-phone-beauty-final` scored **18/18**, zero browser errors, mean **3.833% MAE** and zero numerical candidates. @Browser inspection confirmed Shop information containment at 320/393/430 and Report dismissal with focus return. Current exact-source CI build, production smoke and the full capture/test run remain pending; no local production build was run alongside the owned dev preview.

## Remaining source and verification work

All 307 standalone images are reconciled to existing ordered states (179 exact hash matches, 128 inspected export variants); they add no new screen or clearer asset. [The implementation map](../shop-implementation-map.md#open-source-obligations) records exact inventories and the complete sampled review of 16 videos. The current playback implementation does not establish exact continuous-motion or audio acceptance.

Local original-media verification still rejects **`store-hero`**, **`shower-caddy`** and **`rice-shampoo`** against their unchanged expected hashes. Missing Pura/Chemical Guys motion, partial or occluded photography, incomplete source-specific catalog/history states and source typography differences remain visible obligations. Beauty caption removal changes alpha within declared bounds; it cannot recover the photograph hidden by the original text. Existing crops or successful rendering do not repair original provenance failures. Keep lawful/private asset boundaries, diagnostic thresholds and frame-specific platform exceptions intact.

Shopify reference acquisition remains separately unresolved in [its source record](../shopify/README.md); unknown totals and unacquired assets are not buyer-parity completion.

## Next action

Implement Receipt spacing/payment information, source-specific Orders recommendations, remaining receipt/recommendation returns and checkout thumbnail/rounded-star variants. Then shared PDP/review finishing, Home accessory hearts and PDP collection creation history; Saved options/edit/public/delete stage history follows. Root Browser confirms both collection-stage gaps in ignored `20260920-pdp-collection-root-audit.json` and `20260920-saved-stage-root-audit.json`. Preserve source Cancel semantics and one writer. Continue every unresolved family; passing tests do not establish1:1.

## Dated setup and integration evidence

The [2026-09-12 status at `4d0539f`](https://github.com/darkapoparka/treido-bg/blob/4d0539f2d0cd70f884941cc83f05ff075365542d/docs/STATUS.md) preserves the synchronization history, desktop skill/MCP checks, standalone CLI incompatibility, runtime versions, documentation/upstream checks, earlier Actions results, cache provenance and drive/runtime recovery evidence. Its stopped-preview prerequisite, 195-definition count and older CI outcomes are historical context. [The implementation map's history](../shop-implementation-map.md#historical-evidence) retains the corresponding source/run provenance and earlier visual measurements.
