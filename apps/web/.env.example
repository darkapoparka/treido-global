# Task 1 requires no environment variables or provider accounts.
# Server credentials belong only in apps/web/.env.local when Task 4 implements them.
# NEXT_PUBLIC_* values are public and bundled into browser code.
