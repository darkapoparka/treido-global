# Native foundation

Run `pnpm dev:mobile` from the repository root. See [the stack guide](../../techstack.md) for versions and checks. Metro port: 8081.

This official create-expo-app scaffold contains only a neutral Expo Router route. iOS/Android JavaScript exports do not prove native compilation or device behavior.
