# Treido native scope

Read root AGENTS.md and native.md before native work. This is the Expo buyer app, not the seller CMS. Current Shop mobile-web sessions do not authorize native product changes.

Use the exact installed SDK guidance at https://docs.expo.dev/versions/v57.0.0/ and package pins. Preserve the compatible React Native/React set; never force it to web preferences. Share safe contracts, not server modules or DOM components. Exports are not installed builds or store acceptance.
