# Task 1 requires no environment variables or provider accounts.
# Task 4 will introduce EXPO_PUBLIC_API_ORIGIN with validation and real API use.
# A physical device needs a reachable LAN/HTTPS origin; localhost is the device itself.
# Never place server credentials in EXPO_PUBLIC_* values.
